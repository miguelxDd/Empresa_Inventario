APP_NAME="Empresa Inventario"
APP_ENV=local
APP_KEY=base64:hPTWRvIwhPfQaaM7awzZ/z+o9eshMbD3NMOD/kLORow=
APP_DEBUG=true
APP_URL=http://localhost:8080

APP_LOCALE=es
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=es_ES

APP_MAINTENANCE_DRIVER=file
PHP_CLI_SERVER_WORKERS=4
BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Configuración de base de datos para Docker
DB_CONNECTION=mysql
DB_HOST=mysql
DB_PORT=3306
DB_DATABASE=empresa_inventario
DB_USERNAME=root
DB_PASSWORD=root_password

# Configuración de Redis para Docker
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Configuración de Cache usando Redis
CACHE_STORE=redis
CACHE_PREFIX=empresa_inventario

# Configuración de Sesiones usando base de datos
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

# Configuración de Broadcasting
BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local

# Configuración de Queue usando Redis
QUEUE_CONNECTION=redis

# Configuración de Mail para desarrollo (Mailhog)
MAIL_MAILER=smtp
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@empresa-inventario.com"
MAIL_FROM_NAME="${APP_NAME}"

# Configuraciones adicionales
VITE_APP_NAME="${APP_NAME}"

# Configuraciones de desarrollo
DEBUGBAR_ENABLED=true
TELESCOPE_ENABLED=false